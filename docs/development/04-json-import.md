# 节点 4：JSON 导入与去重

状态：进行中  
返回：[开发总览](README.md)

交付：系统文件选择器、完整校验、预览、内容指纹、重复判断和事务提交。

开发清单：

- [x] 用 Activity Result API 接入系统 JSON 文件选择器。
- [x] 实现限量读取、UTF-8 解析、DTO 校验和带位置的错误信息。
- [x] 实现规范化编码与 SHA-256 指纹，并校验等价文件的结果一致。
- [x] 实现重复查询、同名提醒和导入预览状态。
- [x] 在 Room 事务中写入卡组、卡片、初始位置与原始指纹。
- [x] 处理重复提交的唯一索引冲突，返回已有卡组。

| 用例 | 操作与预期 | 状态 |
|---|---|---|
| I01 | 导入[格式示例](../reference/import-format.md)；卡片按数组顺序建立，默认位置指向第一张。 | 未执行 |
| I02 | 文件为空、JSON 损坏、缺必填字段、字段类型错误或卡片超过上限；显示具体错误且零写入。 | 未执行 |
| I03 | 两份 JSON 仅文件名、字段顺序、未知字段或可选空值写法不同；第二次识别为重复并打开已有卡组。 | 未执行 |
| I04 | 导入后修改预览名称，或编辑已导入卡组后再导入原文件；均不产生原内容副本。 | 未执行 |
| I05 | 名称相同但卡片内容不同；显示提醒，确认后新增卡组，不覆盖旧卡组。 | 未执行 |
| I06 | 两次导入同时提交相同内容；唯一索引保证只创建一个卡组，另一操作打开已有卡组。 | 未执行 |
| I07 | 导入写入中失败，或文件超过 2 MiB；无部分数据，可重新选文件。 | 未执行 |
