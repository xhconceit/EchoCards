# 节点 6：自动播放与自动跟读

状态：未开始  
返回：[开发总览](README.md)

交付：Learning Engine 状态机、真实语音事件驱动、跟读匹配和异常恢复。

开发清单：

- [ ] 实现纯 Kotlin `LearningEngine` 状态机及其 `StateFlow`。
- [ ] 接入自动播放的真实 TTS 完成事件、翻面和下一张流程。
- [ ] 接入自动跟读的识别事件、匹配判断和事务保存。
- [ ] 实现完成锁、`operationId` 失效与重复回调保护。
- [ ] 实现暂停、重读、切模式、后台与音频中断恢复路径。
- [ ] 实现权限、语音服务和保存失败时的可重试 UI 状态。

| 用例 | 操作与预期 | 状态 |
|---|---|---|
| A01 | 自动播放在 TTS 完成前保持正面；完成后翻面，停留后进入下一张。 | 未执行 |
| A02 | 自动跟读在朗读期间不收音；朗读完成后提示用户跟读并开启识别。 | 未执行 |
| A03 | 用户只读开头、漏读结尾或重复部分内容；不自动判定完成。完整读完才前进一张。 | 未执行 |
| A04 | 相同完成回调到达两次；只保存一条完成记录、只前进一张。 | 未执行 |
| A05 | 暂停、重读、切卡或切模式后收到旧 `operationId` 回调；当前状态不受影响。 | 未执行 |
| A06 | 跟读完成记录保存失败；不翻面、不前进，显示可重试错误。 | 未执行 |
| A07 | 后台、音频中断或退出；停止朗读和收音，返回前台保持暂停。 | 未执行 |
