# 节点 2：Room 与数据基础

状态：进行中  
返回：[开发总览](README.md)

交付：表、DAO、Repository、数据库 schema 导出、设置默认值和迁移测试基础设施。

开发清单：

- [x] 在 Version Catalog 中锁定 Room、ViewModel、协程和测试依赖。
- [x] 实现五张表的 Entity、DAO、索引、外键及数据库类，导出 schema。
- [x] 实现卡组、卡片、位置、记录和设置 Repository。
- [x] 实现删除级联、排序、批量写入和学习记录的事务边界。
- [x] 建立数据库迁移测试基础设施，并覆盖首个发布版本的 schema；后续版本增加显式 Migration 与升级用例。

| 用例 | 操作与预期 | 状态 |
|---|---|---|
| D01 | 创建卡组和多张卡片后强制结束并重启 App；内容、顺序和设置保持一致。 | 未执行 |
| D02 | 删除卡组；其卡片、位置和学习记录级联清除，其他卡组不受影响。 | 未执行 |
| D03 | 在批量写入中注入失败；事务回滚，数据库不留下部分卡片。 | 未执行 |
| D04 | 修改正文或跟读文本使版本增加 1；只改标题、记忆点或排序时版本不变。 | 未执行 |
| D05 | 从已发布测试 schema 升级；旧数据保留且新约束生效。首个发布版本前先验证 schema 导出。 | 首个版本 schema 校验已覆盖（设备测试待执行） |
